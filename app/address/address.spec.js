'use strict';

describe('myApp.address module', function() {

  beforeEach(module('myApp.address'));

  describe('address controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addressCtrl = $controller('AddressCtrl');
      expect(addressCtrl).toBeDefined();
    }));

  });
});
